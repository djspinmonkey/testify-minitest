source 'http://rubygems.org'

gem 'testify', '~> 1.0'
gem 'minitest', '~> 2.7.0'

group :test do
  # Since we're monkeying with minitest, it's easiest to use a different framework to test the adaptor.
  gem 'rspec', '~> 2.0'
end
